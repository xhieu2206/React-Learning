const initState = {
  token: '',
  isLoggedIn: false,
  username: ''
}

const reducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default reducer;
