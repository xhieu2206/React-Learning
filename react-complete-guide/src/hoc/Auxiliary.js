// đối với window, tên của component này nên đặt là Auxiliary.js, còn đối với Mac là Aux
const auxiliary = props => props.children; // children là một property đơn giản output tất cả những gì bên trong openning và closing tag

export default auxiliary;
